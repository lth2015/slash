cases:
  - name: "happy path"
    input: "/cluster prod list deploy --ns api"
    mock:
      stdout: "happy.json"
      exit: 0
    expect:
      state: "ok"
      outputs_len: 2
      outputs_row0:
        metadata.name: "web"
        spec.replicas: 4
