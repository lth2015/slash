cases:
  - name: "plan staged with before replicas captured"
    input: "/cluster scale web --replicas 10 --ns api --reason \"traffic spike\""
    mock:
      stdout: "replicas-4.txt"
      exit: 0
    expect:
      state: "awaiting_approval"
      danger: false
      profile_name: "harness"
      has_before: true
      has_after: true
      has_rollback_command: true
