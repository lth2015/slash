cases:
  - name: "plan staged"
    input: "/app rollback web --ns api --reason \"incident INC-4472\""
    expect:
      state: "awaiting_approval"
      danger: true
      profile_name: "harness"
      has_rollback_command: false
