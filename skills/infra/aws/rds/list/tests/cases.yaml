cases:
  - name: "happy path"
    input: "/infra aws rds list --region us-east-1"
    mock:
      stdout: "happy.json"
      exit: 0
    expect:
      state: "ok"
      outputs_len: 2
      outputs_row0:
        DBInstanceIdentifier: "prod-orders"
        DBInstanceStatus: "available"
