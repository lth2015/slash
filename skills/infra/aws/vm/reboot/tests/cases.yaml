cases:
  - name: "plan staged"
    input: "/infra aws vm reboot i-0abc --reason \"kernel hang\""
    expect:
      state: "awaiting_approval"
      danger: false
      profile_name: "harness"
      has_rollback_command: false   # reboot cannot be directly undone
