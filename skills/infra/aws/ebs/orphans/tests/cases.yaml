cases:
  - name: "happy path"
    input: "/infra aws ebs orphans --region ap-northeast-1"
    mock:
      stdout: "happy.json"
      exit: 0
    expect:
      state: "ok"
      outputs_len: 2
      outputs_row0:
        VolumeId: "vol-0aaa111bbb222ccc3"
        State: "available"
