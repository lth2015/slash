cases:
  - name: "plan staged"
    input: "/infra gcp vm reboot web-1 --zone us-central1-a --reason \"kernel hang\""
    expect:
      state: "awaiting_approval"
      danger: false
      profile_name: "harness"
      has_rollback_command: false   # reset is not invertible
