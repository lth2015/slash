# Provider Capability Matrix
# See docs/03-architecture.md §4 for semantics.
# (noun, verb) tuples are what each provider can fulfill. Anything absent
# here produces `UnsupportedOperation` at preflight.
#
# M0 note: this file is a stub. Real values land in M2 alongside provider code.
aws:
  - [vm, list]
  - [vm, get]
gcp:
  - [vm, list]
  - [vm, get]
k8s:
  - [pod, list]
  - [pod, get]
  - [pod, logs]
  - [deployment, scale]
