# Audit Journal

Append-only Markdown record of every `write` run. Each run emits one commit.
See `docs/05-security-audit.md` §6.

M0: empty placeholder. Writers land in M3 together with the Executor.
